<div class="container series-show">

<div class="row mb-3">
  <div class="col-12 mt-3">
    <h1 class="mt-4">
      <%= title_link(@series.title, @series.id) %>
    </h1>
  </div>
</div>

<div class="row mb-3">
  <div class="col">
    <%= @series.description if @series.description.present? %>
  </div>
</div>

  <%= render "/books/books" %>

</div>