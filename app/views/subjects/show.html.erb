<div class="container subjects-show">

  <div class="row">
    <div class="col mt-3 px-0">
      <h1 class="mt-3">
        <%= title_link(@subject.title, @subject.id) %>
      </h1>
    </div>
  </div>

  <%= render "/books/books" %>

</div>
