 <div class="neh-book row">

  <div class="col col-3 col-lg-2">
    <%= link_to image_tag(oabook.show_image("image")), north_broad_book_path(oabook) %>
  </div>

  <div class="col col-9 col-lg-10">
    <%= link_to oabook.title, north_broad_book_path(oabook), class: 'oatitle' %><br>
    <% unless oabook.subtitle.nil? || oabook.subtitle == ''%>
      <em><%= oabook.subtitle %></em><br>
    <% end %>
    <% unless oabook.author.nil? %>
      <span style="color: #a0a0a0;"><%= oabook.author %></span><br>
    <% end %>
    <div class="oasupplemental">
      <%= oabook.supplemental %><br>
    </div>
  </div>
  
</div>