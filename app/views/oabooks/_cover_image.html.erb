<% if oabook.image.attached? %>
  <%= link_to (image_tag oabook.show_image("image")), edit_admin_oabook_path(oabook), {class: "mb-4"} %>
<% else %>
  <%= link_to (image_tag "default-book-cover-show.png"), edit_admin_oabook_path(oabook), {class: "mb-4"} %>
<% end if current_user %>

<% if oabook.image.attached? %>
  <%= image_tag oabook.show_image("image"), class: "mb-4", alt: cover_alt_text(oabook) %>
<% else %>
  <%= image_tag "default-book-cover-show.png", class: "mb-4", alt: cover_alt_text(oabook) %>
<% end unless current_user %>
